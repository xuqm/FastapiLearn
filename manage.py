# coding=utf-8

import uvicorn

if __name__ == '__main__':
    uvicorn.run(app="part16.main:app", host="127.0.0.1", port=8000, reload=True)
    # uvicorn.run(app="part15.main:app", host="127.0.0.1", port=8000, reload=True)
    # uvicorn.run(app="part14.main:app", host="127.0.0.1", port=8000, reload=True)
    # uvicorn.run(app="part13.main:app", host="127.0.0.1", port=8000, reload=True)
    # uvicorn.run(app="part12.main:app", host="127.0.0.1", port=8000, reload=True)
    # uvicorn.run(app="part11.main:app", host="127.0.0.1", port=8000, reload=True)
    # uvicorn.run(app="part10.main:app", host="127.0.0.1", port=8000, reload=True)
    # uvicorn.run(app="part9.main:app", host="127.0.0.1", port=8000, reload=True)
    # uvicorn.run(app="part8.main:app", host="127.0.0.1", port=8000, reload=True)
    # uvicorn.run(app="part7.main:app", host="127.0.0.1", port=8000, reload=True)
    # uvicorn.run(app="part6.main:app", host="127.0.0.1", port=8000, reload=True)
    # uvicorn.run(app="part5.main:app", host="127.0.0.1", port=8000, reload=True)
    # uvicorn.run(app="part4.main:app", host="127.0.0.1", port=8000, reload=True)
    # uvicorn.run(app="part3.main:app", host="127.0.0.1", port=8000, reload=True)
    # uvicorn.run(app="part2.main:app", host="127.0.0.1", port=8000, reload=True)
    # uvicorn.run(app="part1.main:app", host="127.0.0.1", port=8000, reload=True)
    # uvicorn.run(app="application.main:app", host="127.0.0.1", port=8000, reload=True)
